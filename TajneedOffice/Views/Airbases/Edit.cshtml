@model TajneedOffice.Models.Airbase

@{
    ViewData["Title"] = "تعديل القاعدة الجوية";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">تعديل القاعدة الجوية</h3>
                </div>
                <div class="card-body">
                    <form asp-action="Edit">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <input type="hidden" asp-for="AirbaseId" />
                        
                        <div class="form-group">
                            <label asp-for="AirbaseName" class="control-label">اسم القاعدة الجوية</label>
                            <input asp-for="AirbaseName" class="form-control" />
                            <span asp-validation-for="AirbaseName" class="text-danger"></span>
                        </div>
                        
                        <div class="form-group mt-3">
                            <input type="submit" value="حفظ" class="btn btn-primary" />
                            <a asp-action="Index" class="btn btn-secondary">العودة للقائمة</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
} 